#pragma once
#include "소스.h"

int main()
{
	//0.vInt( 1, 2, 3을 넣고, 더한 값을 모니터에 출력)
	// #더할 때, for문으로 더함
	//0.1. 3을 제거하고 나머지 더한 값도 출력
	vector<int> vInt;
	
	vInt[0] = 1;
	

	//1.
	//##A-> B -> C##// 단방향으로 wafer을 옮겨야함
	//A에서 하나로 시작해서 C에서 끝날 때, 하나를 가지고 있도록 함

	A a;
	a.SetWaferCount(1);


	cout << "A wafer : " << endl;
	cout << "B wafer : " << endl;
	cout << "C wafer : " << endl;

}